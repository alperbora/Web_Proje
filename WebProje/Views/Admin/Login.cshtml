@{
    ViewData["Title"] = "Giriş";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header text-center bg-primary text-white">
                    <h3>Giriş</h3>
                </div>
                <div class="card-body">
                    @if (TempData["ErrorMessage"] != null)
                    {
                        <div class="alert alert-danger">
                            @TempData["ErrorMessage"]
                        </div>
                    }
                    <form method="post" asp-action="Login" asp-controller="Admin">
                        <div class="form-group mb-3">
                            <label for="email">E-posta</label>
                            <input type="email" class="form-control" id="email" name="email" placeholder="E-posta adresinizi girin" required>
                            <small class="form-text text-muted">Mail : b221210307@sakarya.edu.tr</small>
                        </div>
                        <div class="form-group mb-3">
                            <label for="password">Şifre</label>
                            <input type="password" class="form-control" id="password" name="password" placeholder="Şifrenizi girin" required>
                            <small class="form-text text-muted">Şifre : sau</small>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Giriş Yap</button>
                        <div class="text-center mt-3">
                            <p>Hesabınız yok mu? <a asp-action="Register" asp-controller="Admin">Kayıt Ol</a></p>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
